<div class="challenge-item">
  <%= image_tag(challenge.vendor.logo) %>
  <h4><%= challenge.vendor.name %></h4>
  <p><%= challenge.description %></p>
  <div id=<%="countdown-clock-#{challenge.id}"%>></div>
</div>

<hr/>