<div class="container">
	<% @rewards.each_slice(4) do |row_of_four| %>
		<div class="row">
			<% row_of_four.each do |reward| %>
				<div><%= reward.title %></div>
			<% end %>
		</div>
	<% end %>
</div>